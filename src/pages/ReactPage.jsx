import React from "react";
import PageStruct from "../components/PageStruct";
import ReactSources from "../sources/react";

const ReactPage = () => {
  return <PageStruct sources={ReactSources} />;
};

export default ReactPage;
